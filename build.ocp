begin program "llvm"

  dirname = ["src"]

  files = [
    "LLVM.ml";
    "llvm_lexer.mll";
    "llvm_parser.mly";
    "printer.ml";
    "main.ml"
  ]

  ocamlyacc = [ "menhir" "--explain" ]

end

begin program "llvm_ocp"

  dirname = ["src"]

  files = [
    "LLVM.ml";
    "llvm_lexer.mll";
    "llvm_parser.mly";
    "printer.ml";
    "llvm_ocp.ml";
  ]

  ocamlyacc = [ "menhir" "--explain" ]

end